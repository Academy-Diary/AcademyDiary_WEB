import React from 'react';
import { Typography } from '@mui/material';

export default function LogoTitle({ mt, mb }) {
  return (
    <Typography variant="h4" align="center" sx={{ mt, mb }}>
      아카데미 다이어리
    </Typography>
  );
}
